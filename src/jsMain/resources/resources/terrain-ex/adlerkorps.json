{
  "efile": "ADLERKORPS",
  "source_absent": "Panzer Marshal original, not an OG efile -- no TerrainEx.txt exists upstream",
  "_note": "Placeholder written by tools/og-import/write_absent_efile_data.py. Every section is empty on purpose: the efile declares nothing, so each Kotlin accessor falls back to its own documented default. Not a stub to be filled in -- the upstream file does not exist. Re-running the real importer overwrites this.",
  "terrain": {},
  "supply_modifiers": {},
  "terrain_cost": {},
  "roads_cost": {},
  "rails_cost": {}
}
